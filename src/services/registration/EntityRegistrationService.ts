import { AxiosResponse } from "axios";
import request from "../request";
// import {PaginationRequestDto,  } from "../../models/newsletters/NewsletterModel";
// import { ApproveEntity } from "../../models/registration/ApproveRegistration";

export const EntityRegistrationService = {
    // getRegisteredEntities: async (model: PaginationRequestDto): Promise<AxiosResponse<any>> => {
    //     return await request.post<any>(`/api/merchant-portal/registration/all`, model);
    // },
    // approveRegistration: async (model: ApproveEntity): Promise<AxiosResponse<any>> => {
    //     return await request.post<any>(`/api/merchant-portal/registration/change-status`, model);
    // }
}
